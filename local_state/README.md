# Local state

Everything in this directory except this README is ignored. Use it for bounded,
rebuildable run output and raw agent streams. Never store a credential here just
because the directory is ignored.
