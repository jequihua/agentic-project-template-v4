# Docs

This folder holds framework guidance and project roadmap artifacts.

Keep broad framework background in `template_framework/`.

For a human-owner introduction to the full template, start with
`template_framework/human_user_manual.md`.

Keep current project state in `PROJECT_STATE.md`, not here.
