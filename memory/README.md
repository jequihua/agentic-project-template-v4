# Optional Memory Lane

Status: inactive unless `PROJECT_STATE.md` sets memory mode to `lightweight` or
`llloom`.

Default: no memory system is required.

When llloom is enabled, keep the operating posture in
`05_governance/current/memory_posture.md` and local paths in
`LOCAL_STATE_NOT_COMMITTED.md`.

